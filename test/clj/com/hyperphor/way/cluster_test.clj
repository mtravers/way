(ns com.hyperphor.way.cluster-test
  (:use clojure.test)
  (:use hyperphor.way.cluster)
  (:require [clojure.string :as str]
            [org.candelbio.multitool.math :as math]))


(defn matrix->maps
  [m fx fy fv]
  (for [i (range )])
  )

(deftest tiny-cluster
  (let [data {:x 1 :y 1 :v 0}]))
